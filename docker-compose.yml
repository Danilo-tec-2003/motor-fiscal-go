services:
  motor-fiscal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: motor-fiscal
    ports:
      - "8080:8080"
    environment:
      APP_PORT: "8080"
      APP_ENV: ${APP_ENV:-development}
